#!/bin/python

from typing import Dict, Iterator, List, Union

# Types
String = str
Integer = int
Float = float
Number = Union[int, float]
IntList = List[Integer]
NumList = List[Number]

Matrix = List[IntList]
